[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
# DB-level UNIQUE index on Cheque Reconciliation.cheque (a Dynamic Link cannot
# be marked unique in meta — Edit DocType validation rejects it). Patch runs
# once; the after_migrate hook re-asserts the index after every subsequent
# migrate, since schema sync drops indexes the meta no longer declares.
treasury.patches.add_unique_index_cheque_reconciliation